const nickname = "Timmy";
const firstname = "Timothy";

console.log("Good Morning", `${nickname}` && `${firstname}!`);

// good morning

// nickname && || firstname

// if nickname is present

// nickname OVER  firstname

// if no nickname is present

// firstname Over all . perhaps else

/*************************************/

// console.log expression Needs to be changed

// if neither a firstname nor nickname is present only Good morning!
